{% extends 'layouts/base.html.twig' %}

{% block title 'Show sections!' %}

{% block body %}
<style> 
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5;padding: 2px 6px; }
</style>
<div class="container" style="margin: auto 0">
<table class="table table-bordered border-primary text-center" style="max-width:700px; margin:100px auto">
<tr class="bg-primary text-white">
<th>First Name </th>
<th>Last Name </th>
<th>Email </th>
<th>Role </th>
{# {{ dump(clients) }} #}
</tr>
{% for client in pagination %}
<tr>
    <td>{{ client.firstName}}</td>
    <td>{{ client.lastName}}</td>
    <td>{{ client.email}}</td>
    <td>{{ client.roles[0]}}</td>
</tr>
{% endfor %}
</table>
{{include('layouts/partials/_pagination.html.twig')}}

{% endblock %}